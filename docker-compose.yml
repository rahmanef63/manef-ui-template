version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ui-template-web
    ports:
      - "3000:3000"
    environment:
      # Inject necessary environment variables, modify as needed
      - NODE_ENV=production
      - NEXT_PUBLIC_CONVEX_URL=${NEXT_PUBLIC_CONVEX_URL}
      - AUTH_SECRET=${AUTH_SECRET}
    restart: always

# Convex runs as a managed service on the cloud so a local container is not strictly needed.
# If you want to run `convex dev` locally you can do so outside of this docker compose.
